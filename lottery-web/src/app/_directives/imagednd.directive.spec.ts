import { ImagedndDirective } from './imagednd.directive';

describe('ImagedndDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagedndDirective();
    expect(directive).toBeTruthy();
  });
});
