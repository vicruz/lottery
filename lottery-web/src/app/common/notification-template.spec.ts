import { NotificationTemplate } from './notification-template';

describe('NotificationTemplate', () => {
  it('should create an instance', () => {
    expect(new NotificationTemplate()).toBeTruthy();
  });
});
